{
  "name": "openfn-language-registry-stack",
  "version": "0.1.0",
  "private": true,
  "description": "OpenFn local adaptor monorepo for Registry Stack services.",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "npm --workspace @openfn/language-registry-notary run check",
    "test": "npm --workspace @openfn/language-registry-notary test",
    "pack:dry-run": "npm --workspace @openfn/language-registry-notary pack --dry-run"
  },
  "engines": {
    "node": ">=20.18.1"
  }
}
