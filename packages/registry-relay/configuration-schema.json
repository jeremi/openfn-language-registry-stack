{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "relay_base_url",
    "token"
  ],
  "properties": {
    "relay_base_url": {
      "title": "Registry Relay URL",
      "type": "string",
      "description": "Base URL for the Registry Relay service.",
      "format": "uri",
      "minLength": 1,
      "examples": [
        "https://agriculture-relay.lab.registrystack.org"
      ]
    },
    "token": {
      "title": "Bearer Token",
      "type": "string",
      "description": "Bearer token or API key for the Registry Relay caller credential.",
      "writeOnly": true,
      "minLength": 1
    }
  }
}
